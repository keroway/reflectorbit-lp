# osv-scan (keroway/.github の reusable-osv-scan.yml 経由) の抑制設定。
# adm-zip 0.6.0 系の GHSA-vwc7-r8mq-g2x9 を受容していた ignore は、
# 別件 GHSA-7q85-xj36-vmfc を修正した 0.6.1 への更新 (pnpm-workspace.yaml overrides) で
# 両方とも解消したため削除した。#244 参照。
